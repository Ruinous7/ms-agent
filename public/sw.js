// This is a placeholder service worker file.
// next-pwa will generate the actual service worker file during build.

// If you need to customize the service worker, you can do so here.
// For most cases, the default service worker provided by next-pwa is sufficient. 